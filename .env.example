# TaskGenie Environment Configuration
# Copy this file to .env and customize values as needed
# Environment variables take precedence: env vars > .env > config.toml > defaults

# ============================================================================
# Application Settings
# ============================================================================

# Application name
APP_NAME=TaskGenie

# Application version
APP_VERSION=0.1.0

# Enable debug mode (shows detailed logs, enables hot-reload)
DEBUG=false

# ============================================================================
# Data Directory Configuration (PR-001)
# ============================================================================

# Override the default app data directory (~/.taskgenie)
# This is where database, cache, logs, and vector store are stored
# TASKGENIE_DATA_DIR=~/.taskgenie

# ============================================================================
# Database Configuration (PR-001)
# ============================================================================

# Database connection URL
# SQLite examples:
#   sqlite+aiosqlite:///path/to/taskgenie.db          (file-based, custom path)
#   sqlite+aiosqlite:///:memory:                    (in-memory database for testing)
# Default (if not set): ~/.taskgenie/data/taskgenie.db
# DATABASE_URL=sqlite+aiosqlite:///path/to/taskgenie.db

# ============================================================================
# Server Configuration
# ============================================================================

# Server host address (default: 127.0.0.1)
HOST=127.0.0.1

# Server port (default: 8080)
PORT=8080

# ============================================================================
# Configuration File Override (PR-001)
# ============================================================================

# Override default config.toml path (~/.taskgenie/config.toml)
# TASKGENIE_CONFIG_FILE=~/.taskgenie/config.toml

# Override .env file path (default: .env in repository root)
# TASKGENIE_ENV_FILE=.env
